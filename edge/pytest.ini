[pytest]
# Ignore les scripts de test manuels (nécessitent caméra/broker MQTT actifs,
# ne peuvent pas tourner dans un pipeline CI/CD comme Jenkins).
addopts = --ignore=tests/manual
testpaths = tests
